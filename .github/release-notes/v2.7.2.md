## 3dslibris 2.7.2

Library sorting improvements. You can now sort your library by six different criteria.

### New Features

* #93, #132: library sort now supports six modes: **by title**, **by filename**, **by author**, **by file type**, **by date modified**, and **by recently opened**. Cycle through them from the library settings button. The last-opened timestamp is saved per book so the recently opened order survives restarts.

### Bug Fixes

* #130: fixed a regression introduced in 2.7.0 where cover images in EPUB books appeared smaller than full screen. CSS margin processing on container elements was offsetting the image start position enough to prevent page-fill mode from triggering.
* #128: improved EPUB block-image pagination continuity for wide landscape chapter images. The image layout planner now avoids an over-aggressive pre-advance in these cases, reducing blank right-screen jumps and preventing unnecessary image/text separation.
* #132: fixed recently opened ordering inside folder views. Books recreated during folder scans now restore their saved `lastOpened` value from prefs, so the sort no longer resets to all zeros after re-entering a folder.
* #132: fixed delayed recently opened refresh when leaving a book. Returning to the library now re-sorts immediately in recently opened mode, without requiring a manual folder reload.

### Included assets

- `3dslibris.cia`
- `3dslibris-debug.cia`
- `3dslibris.3dsx`
- `3dslibris-debug.3dsx`
- `3dslibris-sdmc.zip` (runtime files only; pair it with the `.3dsx` asset for Homebrew Launcher installs)
- `3dslibris-source.tar.gz`
