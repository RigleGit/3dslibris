## 3dslibris 2.3.1

This patch release focuses on stability and browser/render fixes for the `2.3.0` line.

### Highlights

- Fixed several real-world book-opening failures and lifecycle issues that could leave the app stuck on `opening book ...` or crash when changing state at the wrong time.
- Improved browser stability in both `gallery` and `list` views, including metadata/title warmup and selected-title rendering.
- EPUB handling is more robust for malformed HTML entities and interrupted/cancelled opens.

### User-visible changes

- `opening book ...` can now be cancelled with `B`, `Start`, or `Select`, returning cleanly to the library.
- `list view` no longer stops resolving visible titles after the first few entries.
- `gallery view` no longer corrupts the scrolling title marquee for the selected book.
- EPUB content now handles more named HTML entities used in real books.

### Included assets

- `3dslibris.cia`
- `3dslibris-debug.cia`
- `3dslibris.3dsx`
- `3dslibris-debug.3dsx`
- `3dslibris-sdmc.zip`
- `3dslibris-source.tar.gz`
