#pragma once

#include <stddef.h>

namespace format_limits {

static const size_t kMaxCbzCoverEntryBytes = 64u * 1024u * 1024u;
static const size_t kMaxCbzPageEntryBytes = 64u * 1024u * 1024u;

} // namespace format_limits
